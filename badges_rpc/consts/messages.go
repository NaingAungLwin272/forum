package consts

var NotFoundInDB = "no document with that Id exists"

var CreateUserPointErr = "could not create index for userId"
var UserPointExists = "UserPoint with that userId already exists"

var UserBadgeCreateErr = "could not create index for name"
var UserBadgeExists = "user badge with that userId already exists"

var BadgeCreateErr = "could not create index for name"
var BadgeExists = "badge with that name already exists"
